$(document).ready(function() {
  $('#time-form').on('submit', function(event) {
    var loc = $('location').val();
    $.get(

      function(data) {
        $('#primary').append(

        );
      });
      event.preventDefault();
  }):
});